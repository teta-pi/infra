# Roadmap — TETA+PI development plan

This is our **canonical development plan** and task queue. The manager session keeps
the *status* current; the owner only **adds** to it. Ordered by what unblocks the
most; each backlog item is sized for one focused session.

Status legend: ✅ done · 🔄 in progress · ⏳ queued/blocked · 🔴 blocker · 🟠 important · 🟡 minor.

---

## Current sprint — numbered directions, sub-numbered tasks
Naming: `TTPI · <n> <direction> · <n.m> <task>` (see `docs/workflow.md`). Directions:
**1 backend · 2 mcp · 3 frontend (product UI) · 4 db · 5 devops · 6 manager · 7 github · 8 analytics · 9 server · 10 landing (promo) · 11 backoffice · 12 wordpress (plugin) · 13 gtm (autonomous go-to-market) · 14 camera (PI Camera product) · 15 security (standing red-team)**. Historical ✅ work
(pre-numbering: /auth/register removal, resolve_intent enrich, product metrics,
share button, landing pricing, TWIRA embedding code) lives in `docs/changelog.md`.
File ownership is disjoint so sessions never collide in git.

| n.m | Session (chat title) | Task | Status | Worktree / owns files |
|---|---|---|---|---|
| 1.1 | `1 backend · 1.1 fix private-block leak` | close 🟡 leak in `GET /businesses/{id}/blocks` — owner sees all, others only `is_public` | ✅ done 2026-07-12, PR #10 | `routes/blocks.py` |
| 1.2 | `1 backend · 1.2 registry search logging` | append-only request log in `services/registry/*` → unlocks `registry_search_health` | ⚪ queued | `services/registry/*`, new migration |
| 1.3 | `1 backend · 1.3 verification methods` | **verification rework** (`docs/verification-rework.md`): decouple entity creation from registry (L0 free); registry → optional method; NEW email-control + domain-ownership methods; brand↔legal link endpoint + public disclosure. Document upload: NO backend | ✅ done 2026-07-12, PR #15 | `routes/businesses.py`, `routes/auth.py` (reuse), new `services/verification/*` |
| 1.4 | `1 backend · 1.4 TWIRA source_weight` | per-method trust weights (registry/email/domain/document) feeding T-component; account for the weak email-domain binding noted in known-issues (1.3 review) | ✅ done 2026-07-12, PR #17 (registry 1.0 > document 0.85 dormant > domain 0.75 > email 0.5) | `api/app/twira/*` |
| 1.5 | `1 backend · 1.5 rename resets registry status` | manager-review finding on PR #15: `update_business` keeps `registry_status="verified"` after a rename — reset to `unverified` on name change (see known-issues) | ⚪ queued · small | `routes/businesses.py` |
| 1.6 | `1 backend · 1.6 media path traversal fix` | 🔴 audit #1: `GET /media/local/{file_id}/{filename}` serves files with NO auth and NO path containment — resolve + `is_relative_to(_UPLOAD_DIR)` check. **Most exposed finding, live on prod — fix before anything else touches media** | 🔴 **TOP PRIORITY** · tiny | `routes/media.py` |
| 1.7 | `1 backend · 1.7 verification hygiene bundle` | audit #6 (PATCH keeps `agent_endpoint_verified` after endpoint change → reset), #7 (unauthenticated SSRF-prone `/verify-endpoint` → auth/rate-limit the fetch), #13 (Redis `GETDEL` for code confirm). Same family as 1.5 — can be one session with it | ⚪ queued · small | `routes/businesses.py`, `routes/endpoint_verification.py`, `services/verification/*` |
| 1.8 | `1 backend · 1.8 stop GET-writes; recompute level on write` | audit #8: GETs mutate `updated_at` via `_compute_verification_level` on tracked ORM + unconditional commit; level-filtered search stale until random GET. Persist level reactively on the writes that change it | ⚪ queued | `routes/businesses.py`, `core/database.py` (careful) |
| 1.9 | `1 backend · 1.9 wire bitcoin timestamping` | audit #9: upload routes call a no-op stub, real `submit_bitcoin_timestamp` has zero call sites, confirm-check verifies `sha256("")` (wrong digest). Wire task + fix digest; existing beat/task context only, no new workers | ✅ **fully done 2026-07-26.** Code wired 2026-07-24 (folded into 1.20, api PR #13). Worker/beat deployed 2026-07-25 (session 5.4) surfaced a pre-existing OTS-calendar-submission bug (`cal.submit(ts)` passed the wrong argument type). **Fixed 2026-07-26, api PR #14**: `submit_hash()` now passes the raw digest to `cal.submit()` and merges the returned `Timestamp` instead of passing the local one in. Verified live via `journalctl -u tetapi-celery-worker` — `ots_lifecycle` stamps events with zero calendar warnings. End-to-end pipeline (upload → queue → worker → OTS calendar → `bitcoin_proof` stored) is now live on prod | `routes/media.py`, `workers/tasks/bitcoin.py`, `services/bitcoin.py` |
| 1.10 | `1 backend · 1.10 badge SVG endpoint` | GTM Phase 3 badge loop (spawned by 13.1): `GET /badge/{entity_id}` public unauthenticated SVG, shields.io-style, cached (`Cache-Control` 1h + `ETag`), rate-limited, cheap Redis impression counter (not a full audit row), 404 renders its own cached "unknown" badge | ✅ done 2026-07-18, api PR #10 + hotfix PR #11 (PR #10 shipped a `cast("text")` TypeError — 500 on every request, live-caught by manager within minutes, hotfixed to `cast(Business.id, String)`) — **prod-verified**: 200 real entity (`image/svg+xml`, Cache-Control, ETag present), 404 unknown entity | `teta-pi/api` `routes/badge.py` (new) |
| 1.12 | `1 backend · 1.12 fix /businesses/{id}/preview 500` | 🔴 found in 2.5 live E2E: `GET /businesses/{id}/preview` (agent_preview) 500s → breaks 3/7 MCP tools. **Folded into 1.13** (same resolve→verify→profile chain) | ✅ folded into 1.13 (PR #39) | — |
| 1.13 | `1 backend·mcp · 1.13 MCP traction-readiness` | **PRIORITY — the gate before any MCP registry listing (GTM Phase 0).** Close the whole broken resolve→verify→profile chain, all live-confirmed 2026-07-13: (a) 🔴 `GET /businesses/{id}/preview` → 500 (was 1.12) — breaks `get_profile`/`verify_claim`/`verify_endpoint`; (b) 🔴 audit #2 — `resolve_intent` returns **slug** as `entity_id` (`api/app/api/routes/intent.py:65`, `api/app/intent_graph/resolver.py:98`) while every MCP tool + API path wants **UUID** → flagship resolve→verify unusable; return real `biz.id`, keep slug only for `proof_url`; (c) 🟠 audit #5 — `verified_only` no-op (`mcp/src/index.ts:337` sends `undefined`/`"any"`, never filters) → send a real level; (d) 🟡 audit #16 — `get_profile` renders `undefined` media fields. **Exit criterion: all 7 tools pass a real E2E from Claude Code remote MCP / MCP Inspector against prod.** | ✅ done 2026-07-13, PR #39 (MCP 1.4.0) — E2E VERIFIED on prod: resolve-intent→UUID→get_proof/preview all 200 | `routes/businesses.py` (preview), `routes/intent.py` + `intent_graph/resolver.py` (#2), `mcp/src/index.ts`+`client.ts` (#5,#16) |
| 2.1 | `2 mcp · 2.1 get_proof depth` | roadmap #5: ots_status, btc_timestamp_depth, C2PA chain → MCP 1.3.0 | ✅ done 2026-07-12, PR #9, live on prod | proof route + `mcp/src/*` |
| 2.2 | `2 mcp · 2.2 agent auth design` | roadmap #6: design doc for scoped `pk_live_` agent auth (no code) | ✅ done 2026-07-13, PR #33 (scoped pk_live_ design in decisions.md; 8.3 reuses admin:read) | `docs/decisions.md` only |
| 2.3 | `2 mcp · 2.3 SSE streaming` | roadmap #7 | 🔴 deferred: server load | — |
| 2.4 | `2 mcp · 2.4 usage analytics` | roadmap #8 | 🔴 deferred: server load | — |
| 2.5 | `2 mcp · 2.5 ecosystem hardening + listing` | **owner's end goal for direction 2: a fully working, debugged MCP in the Claude ecosystem (and others).** End-to-end test mcp.tetapi.dev from real clients (Claude Code/Desktop remote MCP, MCP Inspector), fix every protocol/manifest/tool-schema issue found; verify `/.well-known/mcp` + agent.json; then registry listings — official MCP registry, Claude connectors directory, other catalogs (actual submissions owner-approved). No new server features — hardening + distribution only, no sustained load. **Must absorb audit findings: 🔴 #2 (resolve_intent returns slug, every other tool wants UUID — flagship flow broken), 🟠 #5 (`verified_only` no-op), 🟡 #16 (`get_profile` renders undefined media fields), 🟡 #17 (`apiFetch` no timeout)** | ✅ done 2026-07-13, PR #31 (hardened: per-session transport, CORS, 15s timeout, server.json for listings; found new 🔴 backend /preview 500 → 1.12) | `mcp/src/*` (fixes only) + `routes/intent.py` (#2), `.well-known`, listing metadata, `docs/mcp.md` |
| 2.7 | `2 mcp · 2.7 resolve_intent verified_only mapping` | found by the 6.2 re-run (2026-07-18): MCP `teta_resolve_intent` with `verified_only:false` returns empty while the raw `POST /resolve-intent` with the identical payload returns the entity — the tool mishandles the flag (likely only maps the `true` branch after audit #5's fix). Also give `teta_search` and `teta_resolve_intent` consistent flag semantics | ✅ done 2026-07-27, mcp PR #5 (v1.5.1). Root cause: `teta_resolve_intent` never declared `verified_only` in its zod schema at all, so any value was silently stripped and the REST call always used the API's default (`true`) — not a `true`/`false` branch bug, an absent-parameter bug. Now exposes `verified_only` (default `true`, same semantics as `teta_search`) and threads it into `resolveIntent()`. Verified live via a real MCP client (raw JSON-RPC/curl session against `mcp.tetapi.dev`): `verified_only:false` on "artificial intelligence consulting services" returns HELLFIRE Solutions, matching direct REST. **New caveat found while verifying (API-side, not fixed here):** `twira_resolve()` doesn't filter by `verified_only` at all — the flag now only takes effect on the keyword-fallback path (TWIRA path ignores it, and TWIRA is the live path almost always since 5.1) — filed as 2.8 | `teta-pi/mcp` `src/index.ts` |
| 2.8 | `2 mcp · 2.8 verified_only not enforced on TWIRA path` | found while verifying 2.7 (2026-07-27): `app/twira/resolver.py::twira_resolve` in `teta-pi/api` never filters candidates by verification level — `verified_only` (now correctly plumbed end-to-end by 2.7) has no effect whenever the TWIRA-ranked path runs, which is almost always post-5.1. Only the keyword-fallback path (`IntentResolver.resolve`) honours it. Decide + implement: filter TWIRA candidates by `verification_level` when `verified_only=true` | ✅ done 2026-07-28, api PR #15 (merged, deployed). `twira_resolve` now takes `verified_only` (default `true`) and filters `verification_level != "none"` — same semantics as `IntentResolver.resolve`'s keyword-fallback path (chose "any level above none", not a `registry`+ threshold, to keep `teta_search`/`teta_resolve_intent` consistent as required). Verified live: `POST /api/v1/resolve-intent` with `"artificial intelligence consulting services"` + `verified_only:true` now returns empty (HELLFIRE Solutions, `verification_level: "none"`, no longer appears); `verified_only:false` still returns it, unchanged | `teta-pi/api` `app/twira/resolver.py`, `app/api/routes/intent.py` |
| 3.1 | `3 frontend · 3.1 web copy sync` | claim checkbox $21→$25, meta description (About link + title were already correct) | ✅ done 2026-07-12, PR #8 | `web/src/app/claim/page.tsx`, `layout.tsx` |
| 3.2 | `3 frontend · 3.2 drag-to-reorder` | wire block reorder to `blockApi.reorder` (native HTML5 drag + rollback) + admin badge → "FOUNDING LOCKED" | ✅ done 2026-07-12, PR #11 | `web/…/profile/page.tsx`, `admin/page.tsx` |
| 3.3 | `3 frontend · 3.3 camera capture` | → **moved to direction 14 (owner, 2026-07-12): camera is a standalone product, own session line.** See 14.1 | ➡ moved to 14.1 | — |
| 3.4 | `3 frontend · 3.4 verification methods UI` | rework UI (`docs/verification-rework.md`): method chooser (registry/email/domain active; document visible-DISABLED "Coming soon"); brand↔legal link UI; public disclosure of the link on profile + `/e/[slug]` | ✅ done 2026-07-13, PR #26 | claim/profile verification UI |
| 3.5 | `3 frontend · 3.5 claim page: no money` | mirror of 10.2 on app.tetapi.dev (owner 2026-07-13): remove the "$25 when billing launches — lock my founding price" checkbox from `/claim` (both form states), reframe to early access ("Join early access — be first"); `pay_ready` simply not sent (verify POST /claim tolerates absence — flag, don't fix backend). NOTE: coordinate with 3.4 — if 3.4 touches claim UI, land 3.4 first | ✅ done 2026-07-28, web PR #19 — checkbox removed, replaced with "Join early access — be first on the registry." Step 2 (email) turned out to be a single shared block for both Business/Organization and Person (not duplicated per `isPerson`), so one edit covered both states — live-verified both on prod. `claimApi.create` no longer sends `ready_to_pay` in the POST body; confirmed live against `api.tetapi.dev`: 201 then 409 (idempotent), no 422/500 — backend already treats it as optional, no backend change needed | `web/src/app/claim/page.tsx`, `web/src/lib/api.ts` |
| 3.6 | `3 frontend · 3.6 auth store unification + real controls` | audit #10 (`/profile` never reads `useAuthStore` — login/settings users silently unauthenticated with false "Saved"), #11 (claim "domain email" step fully fake, `onClick={()=>{}}` + any 3 chars pass — wire to real `/verify/email/*` or hide; skip if 3.4 already wired it), #12 (no UI calls `businessApi.publish`/`setPrivacy` — build controls or remove dead surface) | ✅ done 2026-07-17, web PR #6 — #10 FIXED (useAuthStore read first everywhere + businessId bootstrap via `businessApi.list` for /login users; live-verified on prod with a real /login session, no duplicate Pi CAM sign-in modal); #11 ALREADY-RESOLVED (fake step removed by 3.7); #12 FIXED (new Publish & Privacy section; publish verified live, setPrivacy correct on the frontend but blocked by the 1.18 PATCH-500 backend bug) | `web/src/app/profile/page.tsx` |
| 3.7 | `3 frontend · 3.7 registry-free registration flow` | **owner 2026-07-14: the claim flow still gates "Business" behind a registry search (`web/src/app/claim/page.tsx` step 1 runs Handelsregister/Companies House lookup when `isBusiness`), even though backend 1.3 already decoupled creation from registry. Owner couldn't find how to register without a registry.** Rework the flow after "Start": (1) FIRST screen = pick type — **Business/Organization** OR **Person** (Person sub-copy lists "journalist · actor · creator · other"); (2) THEN email; (3) THEN continue to create the entity immediately (L0, `registry_status=unverified`) — NO registry step in the path. Registry match becomes an OPTIONAL action LATER (on the profile, via the existing `POST /{id}/verify/registry` + the 3.4 method chooser), never a gate at creation. Keep the existing entity-kind→entity_type mapping but drop the registry-search branch from the create path | ✅ done 2026-07-16, web PR #4 — deployed + verified 200 on prod. Session also found the PATCH 500 root cause: `MissingGreenlet` serializing `updated_at` post-commit (`businesses.py:232`) → folded into 1.18 | `web/src/app/claim/page.tsx`, `useOnboardingStore`, `useProfileStore`, `profile/page.tsx`, `lib/types.ts` |
| 1.15 | `1 backend · 1.15 resolve_intent proof_url dead link (+ update_block embed guard)` | found by 2.6: `resolve_intent` results carry a `proof_url` that 404s (wrong host/path — must be the live public page `https://app.tetapi.dev/e/{slug}`, verify against a real published entity). Bundle the 1.18 follow-up in the same session: `update_block` (`routes/blocks.py:152`) has the same unguarded `generate_embedding` call that made `add_block` 500 (OpenAI billing 429) — wrap in the identical try/except | ✅ done 2026-07-18, api PR #7 — proof_url → `app.tetapi.dev/e/{slug}` (live-verified 200), update_block guarded. Spawned 1.21: the fix lives in the TWIRA branch, which never runs while OpenAI billing is unpaid — the keyword fallback returns `proof_url: null` (prod-confirmed) | `teta-pi/api` `routes/intent.py`, `routes/blocks.py` |
| 1.21 | `1 backend · 1.21 proof_url in keyword-fallback resolve_intent` | ✅ done 2026-07-18, api PR #8 (bundled with S-9) — fallback now sets `proof_url=app.tetapi.dev/e/{slug}`; prod-verified: resolve-intent fallback returns non-empty proof_url that opens 200. **Caveat fully closed 2026-07-27 (5.1): with OpenAI billing paid the TWIRA vector branch now runs live, so prod traffic no longer defaults to the keyword fallback — both paths return a correct non-null proof_url, REST and MCP.** ~~orig:~~  GTM Phase 1 requires proof_url in EVERY response, but with OpenAI billing unpaid ALL prod traffic uses the keyword fallback (`intent_graph/resolver.py`), whose `IntentResolution` leaves `proof_url=None` (prod-confirmed 2026-07-18). Populate it from the entity slug — same `https://app.tetapi.dev/e/{slug}` formula as PR #7. Tiny | 🟢 next api slot · tiny | `teta-pi/api` `intent_graph/resolver.py` (+ schema if field missing) |
| 1.16 | `1 backend · 1.16 verify registration payload for 3.7` | small companion to 3.7: confirm `POST /businesses` + the claim endpoints accept the simplified type-first + email payload with ZERO registry fields required (1.3 already decoupled `create_business`, so likely already true — this is a verification + any-gap-fix task, not a rebuild). Fix anything that still assumes a registry_id/registry_data at create/claim time | ✅ done 2026-07-16 — verified, no gap: `BusinessCreate` has no registry fields at all, `ClaimCreate` is type+email only, DB columns nullable. No code change needed, no PR | `teta-pi/api` `routes/businesses.py`, `routes/claims.py` |
| 1.17 | `1 backend · 1.17 fix /search + /resolve-intent query filtering` | 🔴 **TOP PRIORITY — re-blocks GTM Phase 0.** Found live by 6.2 QA gate (2026-07-16, PR #7): `GET /search` returns the identical result set regardless of the `query` param (empty/exact-match/slug/nonsense all identical); `POST /resolve-intent` (backs `teta_resolve_intent`) returns empty for every query, including exact-name matches on entities that exist. Root-cause both — likely the same class of bug, query text not reaching the filter/match step — and fix. Re-verify live on prod (both raw curl and a real MCP client call) before closing | ✅ done 2026-07-16, api PR #4 — root cause: query text never reached the SQL WHERE, only re-scored the LIMIT'd window. VERIFIED on prod: `/search` correct on all 4 variants (empty→browse, exact→1, slug→1, nonsense→0); `/resolve-intent` filters correctly (exact match returns UUID with `verified_only:false`). 🟡 note: `verified_only` defaults to `true` (schema.py:13) so L0 entities are invisible to default resolve-intent calls — by design, but logged in known-issues as a product question | `teta-pi/api` `app/api/routes/search.py`, `app/intent_graph/resolver.py` |
| 1.18 | `1 backend·3 frontend · 1.18 fix blocks-create 500, PATCH businesses 500, missing search page` | 🔴 **found by 6.2 follow-up (2026-07-16), once a real API key unblocked steps 2/3/4(a)**: (a) backend — `POST /businesses/{id}/blocks` 500s on every payload (any title/description/order combo), route is fully broken, not a validation edge case; (b) backend — `PATCH /businesses/{id}` 500s whenever `is_public` or `is_published` is in the body (isolated field-by-field), which also means the "unpublish via PATCH" fallback for entities with no `DELETE` endpoint doesn't work either; (c) frontend — the homepage search box routes to `app.tetapi.dev/search?q=...`, a page that doesn't exist (404, silent, no error shown) — a working `/search` page (or inline client-side results) needs to exist regardless of the API-side query-filtering fix in 1.17. Full repro + exact isolation steps in `docs/known-issues.md`. **Root-cause lead for (b), from the 3.7 session (2026-07-16): `MissingGreenlet` raised while serializing `updated_at` after commit, `businesses.py:232` — async SQLAlchemy lazy access on an expired instance post-commit; (a) likely the same family (check the blocks-create handler for the same post-commit serialization pattern).** Also: manually hard-delete/hide leftover test entities `44edb26e-bfab-4c25-bbb0-f251b0a1cf5a` and `4cfe5174-f767-46b3-b5fb-e1593c28924d` from prod DB once (b) is fixed or via direct DB access | 🔄 (a)+(b) ✅ done 2026-07-17, api PR #6, **prod-verified**: POST block → 201, PATCH booleans → 200 (root causes: unguarded OpenAI RateLimitError on embed; missing `db.refresh` before serialization). Both test entities hidden via the fixed PATCH — search 0 results, public pages 404, no DB cleanup needed. (c) search page in flight — boot issued (session/1.18c-search-page). 🟡 follow-up filed: `update_block` (blocks.py:152) has the same unguarded embedding call — latent duplicate of (a) | `teta-pi/api` `routes/blocks.py`, `routes/businesses.py` (`update_business`) · `teta-pi/web` — search entry point |
| 1.19 | `1 backend · 1.19 email from-address → tetapi.dev + surface send errors` | follow-up to the Resend domain verification (2026-07-17): (a) `FROM_ADDRESS` in `app/services/email.py:13` still `onboarding@resend.dev` — switch to `TETA+PI <verify@tetapi.dev>` now that the domain is verified (without this, sends to non-owner addresses keep failing despite the verified domain); (b) silent-fail fix: `POST /auth/email-code` returns 200 even when Resend rejects the send (live-observed 401/403 logged next to a 200 response) — return 502/503 with a human detail instead, keep the Resend error text in logs only | ✅ done 2026-07-17, api PR #5 — deployed + live-verified: email-code to a NON-owner address → 200 with no Resend error (route now awaits the send, 502 + cooldown cleanup on failure) | `teta-pi/api` `app/services/email.py`, `routes/auth.py` (email-code path) |
| 3.8 | `3 frontend · 3.8 app P0 fixes (About link, title/meta) + doc-upload state check` | **owner 2026-07-17 (Landing Update master prompt v3), P0:** (a) app.tetapi.dev footer/nav "About" link points at `http://localhost:3000` — fix to `https://tetapi.dev/about.html`; grep the whole repo for other stray `localhost` references while there; (b) `<title>`/meta-description still say "Agent Economy" / "Verified businesses for the agent economy" — update to "TETA+PI — Trust Infrastructure for Digital Entities"; (c) P1 confirm-only: the verification method chooser should already show "Upload a document" as visible-DISABLED "Coming soon" (shipped in 3.4) — verify it's still there and reads as the master prompt wants ("registration certificate, license, tax ID"), adjust the label if not, do NOT wire any upload backend | ✅ done 2026-07-17 — **no-op: all three items verified already fixed on live prod** (manager re-checked independently: title/meta = "Digital Entities", About → tetapi.dev/about.html, zero localhost refs across all pages, zero "Agent Economy"). The master prompt's "confirmed still broken" status was stale. No PR | `teta-pi/web` (no changes) |
| 1.20 | `1 backend·3 frontend · 1.20 blocks become real (QA #7/#16/#20)` | QA 2026-07-17, 2×Critical+1×Medium: (a) file/media upload does not attach to blocks in the UI flow (backend `/media/upload` exists — trace web BlockCard→mediaApi→block chain E2E and fix where it breaks); (b) C2PA/Bitcoin timestamp is a UI label with no real pipeline call — ties into 1.9 (OTS stub unwired, wrong digest); (c) blocks are not addressable (no permalink/view URL per block — needs a public GET + web route) and not findable via search (NOTE, partly by design: `/search` covers entities only; block embeddings need OpenAI billing → 5.1). Scope the split backend/frontend before booting — likely 2 sessions | ✅ **fully done 2026-07-27.** Backend half done 2026-07-24 (api PR #13 — (b)/1.9 wired for real + digest bug fixed; (4)/caveat-(i) media_url/content_hash serialization gap fixed on both public read paths; (c) new public `GET /api/v1/blocks/{block_id}` permalink shipped). **1.20-web done 2026-07-27, web PR #17**: `BlockMedia`/`ProfileBlock` now carry real `{id, storage_url, original_hash, c2pa_verified, bitcoin_confirmed}`; `handleFileUpload` no longer discards `mediaApi.upload`'s response (which itself is missing url/hash — response_model strips them — so it follows up with the new `blockApi.get()` permalink call and matches by `media_id`); `mapServerBlock` populates the same fields on load; `MediaDisplay` renders the real file via same-origin `mediaUrl()` proxy + real truncated hash instead of the hardcoded fake string; new public web route `/e/[slug]/blocks/[blockId]`. Verified live on `hellfire-solutions`'s real 3-media block, both locally and on prod. **Bonus bug found+fixed same day, web PR #18**: the new route 404'd right after PR #17's own deploy — `deploy.yml`'s hand-maintained `app-paths-manifest.json` (same mechanism that bit 3.12's icon routes) didn't list it; this is the second time this exact thing has silently 404'd a clean deploy, worth a root-cause fix later (sync the real manifest instead of hand-copying entries) | `teta-pi/api` `routes/media.py`, `routes/blocks.py`, `routes/businesses.py`, `workers/tasks/bitcoin.py`, `services/bitcoin.py`, `schemas/block.py` · `teta-pi/web` `src/stores/useProfileStore.ts`, `src/lib/api.ts`, `src/lib/types.ts`, `src/app/profile/page.tsx`, `src/app/e/[slug]/blocks/[blockId]/page.tsx` (new), `.github/workflows/deploy.yml` |
| 4.1 | `4 db · 4.1 verification rework migration` | migration: `entities.legal_entity_id` nullable self-FK + extend `verification_events.event_type` enum (`email_verified`, `domain_verified`, `document_verified`); append-only trigger must survive | ✅ done 2026-07-12, PR #14 (migration ran on prod deploy) | new migration + models |
| 5.1 | `5 devops · 5.1 enable TWIRA embeddings` | key → server `.env`, backfill, verify (code already merged) | ✅ done 2026-07-27 — OpenAI billing paid, key live. Backfill (`twira_backfill_block_embeddings` via the running Celery worker) embedded 6/10 public blocks (4 correctly skipped — media-only, no title/description). Embed-on-write verified live: block create → 1536-dim vector immediately, update → re-embeds. Semantic `/resolve-intent` confirmed through the TWIRA branch (not keyword fallback): query "artificial intelligence consulting services" → HELLFIRE Solutions via block "AI solutions for everyone" (I=0.315), `proof_url=app.tetapi.dev/e/hellfire-solutions` (non-null). Same result live via MCP `teta_resolve_intent`. Closes the 1.21 caveat. No code change — docs-only | server `.env` + one-off backfill |
| 5.2 | `5 devops · 5.2 split plan (scope C)` | **owner decided 2026-07-13: scope C — full extraction to separate GitHub repos** (`api` / `web` / `mcp` / `landing` each own repo under `teta-pi`). This task = WRITE the C split plan into `docs/decisions.md` ONLY (zero code, zero deploy): repo layout, how git history is carried per component (`git filter-repo` subtree), per-repo CI/deploy workflow design, cross-repo contracts (web→api URL, mcp→api URL, shared types/agent.json), secrets/`.env` distribution, cutover order + rollback, and how the 512MB droplet deploy model changes. Execution is 5.3+ (gated on server upgrade — do NOT execute here) | ✅ done 2026-07-13, PR #41 — C plan in decisions.md (filter-repo per folder, infra meta-repo, per-repo deploy) | `docs/decisions.md`; worktree `ttpi-wt/5.2-split` (reset to main) |
| 5.3 | `5 devops · 5.3 execute repo split (C)` | execute the approved 5.2 plan: `git filter-repo` extraction per component, create `teta-pi/{api,web,mcp,landing,infra}` repos, per-repo deploy workflows + secrets, cutover in a merge freeze, rollback = re-enable mono deploy. **Prod-affecting (deploy rework + restarts)** | 🟢 unblocked — 9.1 resize landed 2026-07-13 (2GB/50GB); schedule cutover window | new repos + per-repo `.github/workflows/*` |
| 5.4 | `5 devops · 5.4 deploy celery worker/beat` | 1.20-api (2026-07-24) wired real Celery tasks (bitcoin timestamping) but no worker/beat process existed on prod — tasks queued into Redis and were never consumed. Deploy `tetapi-celery-worker`/`tetapi-celery-beat` systemd units so the queue actually drains. **Prod-affecting (new systemd services)** — plan shared with owner, explicit go-ahead given before touching prod | ✅ done 2026-07-25 — two new systemd units (`WorkingDirectory=/opt/tetapi/api`, same venv/`.env` as `tetapi-api`), `celery -A app.workers.celery_app:celery_app worker/beat`, `--concurrency=1` (RAM budget, same philosophy as `uvicorn --workers 1`). Verified live: worker picks up a fresh upload's `submit_bitcoin_timestamp` task within ~10s (was stuck indefinitely before), all 4 beat tasks (`ots_lifecycle`/`probe_endpoints`/`twira_recompute_scores`/`check_bitcoin_confirmations`) fire on schedule without error. **New finding while verifying:** the task ran but still failed at the OTS-calendar-submission step — a separate, pre-existing bug in `services/bitcoin.py` (`cal.submit(ts)` passes the wrong argument type to the installed `opentimestamps-client` library), invisible until this session's worker made the code path reachable for the first time. Flagged as its own follow-up (not fixed here — wrong repo/scope for this session); **fixed 2026-07-26, session 1.9, see `known-issues.md`** — also surfaced that this deploy pipeline doesn't restart `tetapi-celery-worker`/`tetapi-celery-beat` on push, so worker/task code changes need a manual `systemctl restart` after deploy (documented in `deployment.md`) | `/etc/systemd/system/tetapi-celery-worker.service`, `tetapi-celery-beat.service` (server-only, not in this repo) — documented in `deployment.md` |
| 6.1 | `6 manager · 6.1 system-wide bug audit` | read-only sweep api/web/mcp/landing → 17 verified findings in `docs/known-issues.md`; spawned tasks 1.6-1.9, 3.6, 10.3 + absorbed into 2.5 | ✅ done 2026-07-13, PR #23 | `docs/known-issues.md` only |
| 6.2 | `6 manager · 6.2 pre-GTM core flow QA` | **GATE — owner 2026-07-14: must be green before starting GTM Phase 0.** Live E2E on prod, both as a human (web UI) and as an agent (MCP client), no mocking: (1) create a business entity (`POST /businesses`, no registry call — verify 1.3's decoupling still holds); (2) add a block to it; (3) upload a file/image to a block, confirm it lands (media_url/hash set, C2PA if applicable); (4) search finds the new entity — both `app.tetapi.dev` search UI AND `teta_search`/`teta_resolve_intent` via a real MCP client (Claude Code remote MCP or Inspector) return it; (5) `/e/[slug]` public page renders the block + media. Log every defect found into `docs/known-issues.md` with repro steps — do not silently fix in this session unless trivial; this is a QA sweep, not a fix session. Camera sync is OUT of scope here — see 14.2, runs in parallel | 🔴 **GATE FAILED, still RED after follow-up (2026-07-16).** First pass (PR #7): `GET /search` ignores `query`; `POST /resolve-intent` empty for every query; steps 2/3 untested (no auth yet); step 5 partially confirmed. Follow-up pass, once a real API key unblocked steps 2/3/4(a): step 2 fails outright (`POST /businesses/{id}/blocks` 500s on every payload, so step 3 is unreachable too — needs an existing block first); step 4(a) fails independently of the API bug (homepage search box routes to a nonexistent `/search` page, 404, no error shown); and the fallback cleanup path (`PATCH is_public=false`) also 500s, leaving two test entities stuck live on prod (`44edb26e-…`, `4cfe5174-…` — see known-issues). Only step 1 fully passes. New findings filed as 1.18. **RE-RUN 2026-07-18 (manager, authenticated with the owner's test key): 🟢 GREEN with 3 named caveats.** Full chain passed live on prod: (1) create 201 registry-free → (2) block 201 → (3) media upload 200 (needs `type` form field) → (4) search finds it via API, the new `/search` page (200), AND MCP `teta_search` (correct UUID + working proof link) → (5) `/e/[slug]` 200 with the block; cleanup PATCH 200. Caveats (filed, non-blocking): (i) block media serialization exposes empty `media_url`/null `content_hash` → 1.20(a); (ii) MCP `teta_resolve_intent` ignores `verified_only:false` — raw API returns the entity, MCP tool returns empty → new **2.7**; (iii) keyword-fallback `proof_url: null` → 1.21. **GTM Phase 0 is unblocked — owner call on whether caveats (i)–(iii) ship first.** | read-only QA — `docs/known-issues.md`, `docs/roadmap.md` |
| 6.3 | `6 manager · 6.3 investigate phantom entity names in search (QA #32)` | QA session 2 (2026-07-19) #32 originally filed as "delete leftover seed entities" ("Меридіан", "Haiku", "Ant", "Claude Code", "Ant Norvind", "Vega", "Foundry"). **Manager investigated 2026-07-19: these are NOT real data.** `GET /search` for each name returns zero results, and a direct read-only `psql` query against prod (`SELECT ... FROM businesses WHERE name ILIKE ANY(...)`) returns **0 rows** — none of these names exist anywhere in the `businesses` table, published or not. Only 5 real entities currently exist on prod (`bob`, `tetakta`, `shosho`, `Test Reporter`, `HELLFIRE Solutions`). **Reclassified: this is very likely the same bug family as #18/#25/3.11/3.14** — the owner's UI showed fabricated business names (several look like AI-model codenames — Haiku, Vega, Foundry — same texture as the "Bronnows Sonnet Sonnet Sonnet" garbage from #18) that don't correspond to any database row, i.e. a rendering/state bug producing fake entity cards, not stale seed data needing cleanup | 🔴 **merge scope into 3.14** — same investigation, don't fix twice | `teta-pi/web` — folds into 3.14's scope, no separate session |
| 14.5 | `14 camera · 14.5 new build + dual sync points (onboarding + block-creation)` | QA session 2 (2026-07-19) #33, 🔴 Critical, blocks the whole Camera→Blocks flow: (1) basic pairing/record must work as before once 14.4's dev-client is confirmed booting; (2) sync at onboarding via QR/Pairing Code (ties into 3.12's #11 dead-stub fix — wire for real, don't just hide); (3) **NEW requirement — if the camera step was skipped at onboarding, the same QR/Pairing-Code sync must be reachable later from the block-creation step in `/profile`** (ties into 3.13's "Connect Camera near blocks" #30). Hard dep: owner confirms 14.4's dev-client boots on a real device first | 🔴 blocked on owner's 14.4 device confirmation | `teta-pi/pi-cam` · `teta-pi/web` claim onboarding + `profile/page.tsx` blocks section |
| 7.1 | `7 github · 7.1 branch protection` | protect `main`: PRs only, no force-push/delete, enforce_admins | ✅ done 2026-07-12, verified live | GitHub settings only, no code |
| 7.2 | `7 github · 7.2 repo descriptions vs landing` | audit org/repo descriptions + READMEs against current landing copy (Modules, $25, "Digital Entities"); propose diffs, land as one batched PR | ✅ done 2026-08-04 (folded into 7.x) — scope narrowed to the org profile README (`teta-pi/.github/profile/README.md`): added missing `pi-cam`/`wordpress-plugin` rows (was 5 of 7 live repos) and fixed the "Connect an agent" snippet's `/sse`→`/mcp` (wrong path, 404s live). Per-repo description/landing-copy diff not done this session — still open if wanted, small | GitHub metadata + `README.md`s |
| 7.3 | `7 github · 7.3 commit attribution audit` | verify commits across branches/repos show on the owner's GitHub account (noreply email policy), incl. worktree branches | ⚪ queued · read-only | none (gh/git read-only) |
| 7.4 | `7 github · 7.4 CI/CD audit across repos` | **owner-directed, 2026-08-04.** Read-only audit of Bandit/pip-audit (api), npm audit (web), CI gap (pi-cam had none), branch protection uniformity across all 6 public repos + pi-cam. Not a fix session — findings filed to `docs/known-issues.md`/`docs/security.md` (S-11..S-14) with task numbers | ✅ done 2026-08-04 — api Bandit (1 High: MD5-for-ETag, false-positive-flavored) + pip-audit (`ecdsa` PYSEC-2026-1325, no upstream fix, low real risk — JWT uses HS256 not ECDSA) both non-blocking; web npm-audit unchanged from 2026-07-28 (3 vulns, next@15.0.3, fix known, needs a dedicated upgrade session); pi-cam had zero CI — added `npm-audit.yml` (PR #3 merged), CodeQL skipped (private repo + org Free plan = no GHAS, same root cause blocks branch protection on that repo too — the only unprotected repo in the org, owner decision needed: make public or upgrade plan); branch protection confirmed uniform (enforce_admins, no force-push/delete, PR-only) across all 6 public repos, `required_status_checks` unset everywhere → every CI check org-wide is advisory-only by design (matches 15.2) | `docs/known-issues.md`, `docs/security.md` (read-only audit) · `teta-pi/pi-cam` `.github/workflows/npm-audit.yml` (new) |
| 7.5 | `7 github · 7.5 MCP production-readiness audit` | **owner-directed, 2026-08-04.** 9-point read-only audit of `teta-pi/mcp` against real AI-agent traffic: graceful degradation, observability, guardrails, retry/circuit-breaker, auth/security, testing, CI/CD, docs consistency, technical debt. Findings only, no code changes | ✅ done 2026-08-04 — full writeup in `docs/known-issues.md` + `docs/security.md` (S-11..S-14). Headline findings: zero observability (no logging at all — biggest gap), zero tests, README's `auth: Bearer` claim is false (confirmed live, fully anonymous — matches infra's canonical docs but contradicts the mcp repo's own README, which also has a dead `/sse` path), zero rate-limiting on the MCP ingress itself, `sessions` Map has no expiry (slow leak under real sustained traffic), `docs/mcp.md` version stale (1.3.1 vs live 1.5.1). No retry/circuit-breaker but timeouts + error surfacing are sane (confirmed live: 404 → clean `isError` tool response, not a hang/crash) | `docs/known-issues.md`, `docs/security.md` (read-only audit) |
| 7.6 | `7 github · 7.6 org showcase correction` | **owner-directed, 2026-08-04, with a correction to the original ask** (owner wanted `protocol`/`mcp`/`platform`/`infra` pinned — `protocol`/`platform` are archived, pinning them would contradict the task's own goal of matching reality). Pin the real live+public repo set, unpin anything archived, sync org README (see 7.2) | ✅ done 2026-08-04 (partial — API has no repo-pin mutation, GitHub Pin/Unpin is web-UI-only, confirmed via GraphQL schema introspection, no `admin:org` workaround exists). **Found via GraphQL** (`organization(login:"teta-pi"){pinnedItems...}`): currently pinned = `mcp-server` (**archived**) + `api` only (2 of the max 6 slots, not what the owner described either). **Exact manual steps left for the owner** (2 min): go to `github.com/teta-pi` → "Customize your pins" → check `api`, `web`, `mcp`, `landing`, `infra` (+ `wordpress-plugin` for the 6th slot, recommended) → uncheck `mcp-server` → Save. `protocol`/`platform`/`pi-camera` were never pinned (confirmed, not an extra step). Org README already fixed (7.2) | GitHub org settings (owner-executed, no API path exists) |
| 8.1 | `8 analytics · 8.1 dashboard design` | design the super-dashboard: inventory existing metrics (`/admin/stats`, `/admin/analytics`, `/admin/product-metrics`, GoatCounter), define layout + which metrics matter + alert thresholds; DESIGN DOC first, no code | ✅ done 2026-07-12, PR #12 | `docs/analytics.md` |
| 8.2 | `8 analytics · 8.2 build dashboard` | implement the approved 8.1 design in the admin UI (read-only queries on existing endpoints, no new tables/workers) | ✅ done 2026-07-12 (as session 11.1) | `web/src/app/admin/page.tsx`, `web/src/lib/api.ts`, `routes/admin.py` (append-only) |
| 8.3 | `8 analytics · 8.3 metrics notify agent` | agent that polls key metrics and notifies on thresholds (runs OFF-server — scheduled Claude session / local cron hitting read-only admin API); no server-side workers until upgrade | ⚪ after 8.1 · off-server | new scripts/ or scheduled task, read-only API key |
| 9.1 | `9 server · 9.1 capacity audit + upgrade plan` | measure what's eating the droplet (RAM/CPU/disk per service), pick target droplet size + cost, write the upgrade runbook; unblocks 5.1/5.2/2.3 | ✅ done 2026-07-13, PR #38 → resize target `s-1vcpu-2gb` (2GB/50GB, $12/mo); runbook in deployment.md; owner schedules window | server (read-only audit), `docs/deployment.md` |
| 10.1 | `10 landing · 10.1 verification methods copy pass` | after the rework ships: "How it works" + "Verification levels" mention email/domain/document methods | ✅ done 2026-07-27 | `landing/index.html` |
| 10.2 | `10 landing · 10.2 positioning: research lab, no money on hero` | owner 2026-07-13: (1) remove ALL "TetaPi GmbH" claims across landing pages (we are not a company yet) → "research lab / startup" positioning; legal pages (terms/privacy) must stop naming a non-existent GmbH — reword to project/lab operator + contact email; (2) hero: remove money — CTA "Get verified — lock $25 founding price" → "Create your page"; (3) hero-adjacent claim form: KEEP (it feeds claim_stats + GTM Phase-2 outreach) but reframe to early access ("Join early access — be first"), drop the $25 checkbox line from it; pricing SECTION further down stays as is | ✅ done 2026-07-13, PR #29 | `landing/*.html` only |
| 10.3 | `10 landing · 10.3 landing truth pass` | audit 🔴 #3 (developers.html documents non-existent endpoints + wrong base URL — every curl 404s), 🔴 #4 (onboarding.html form posts to placeholder Formspree ID — funnel silently dead; point at /claim), 🟡 #14 (wrong support email `hello@teta-pi.io` ×4), 🟡 #15 (llms.txt wrong agent.json subdomain + says 4 MCP tools, real 7). Coordinate with 10.2 (same files family) — can be one session right after it | ✅ done 2026-07-27 | `landing/developers.html`, `onboarding.html`, `llms.txt`, `for-agents.html` |
| 3.9 | `3 frontend · 3.9 session/auth integrity (QA #1/#2/#4)` | QA 2026-07-17, Critical: (a) #1 expired session still renders `/profile` in full edit mode next day — must detect 401/expired token → clear stores → show signed-out state, never editable UI on a dead session; (b) #2 "Make this page private" → "invalid token" (stale token being sent — same family); (c) #4 Business Email "Send Code" unresponsive — re-test after (a) lands, fix if independent | ✅ done 2026-07-18, web PR #10 — centralized 401 handler in `lib/api.ts` (fires `teta:unauthorized`, clears both stores), `/profile` now gates the whole edit surface behind `sessionInvalid`/`SignedOutPanel` instead of individual buttons; deployed, `/profile` 200. #2/#4 backend paths verified live via curl with a real JWT; the FE 401→signed-out path itself verified via event-dispatch simulation (session sandbox has no outbound network) — flagged by the session for one manual browser click-through as a follow-up | `teta-pi/web` `lib/api.ts`, `profile/page.tsx`, `useProfileStore.ts` |
| 3.10 | `3 frontend · 3.10 verifier UX per entity type (QA #3/#6/#13/#14)` | persona accounts see the full business verifier set (registry/business-email/domain/document) — narrow per entity_type (persona: email+domain); #6 "Legal Entity" link under Document Upload is unclear+inert — wire to `legal_entity_id` properly or remove; #13 Registry Match silently flips to verified with no in-progress/result feedback — add explicit status flow; #14 persona cards in search show business "registry handle number" — hide business fields for persona | ✅ done 2026-07-19, web PR #12 — `VerificationSection` gates Registry Match/Document Upload/Legal Entity behind `isBusinessKind` (persona sees Email+Domain only); #6 kept the Legal Entity link (it's genuinely wired to `legal_entity_id`, just scoped to business now — likely the real source of the "confusing" complaint); #13 resumes polling on mount if status is already `pending`, shows "still processing" instead of reverting to a bare button; #14 homepage search card hides registry fields for non-business entity_types. Live-verified: #14 against real prod data (4 entity types render correctly); #3/#6/#13 verified via code review only — session had no persona/business test-account credentials to click through `/profile` live, flagged for a manual pass. Deployed, `/profile` + home 200 | `teta-pi/web` `profile/page.tsx`, `page.tsx` |
| 3.11 | `3 frontend·1 backend · 3.11 entity state isolation (QA #18)` | 🔴 **Critical: creating a 2nd (business) entity under the same account shows instant "verified in registry" status, garbage text ("Bronnows Sonnet…"), and content leaked from the 1st (persona) entity.** Prime suspect: `useProfileStore` localStorage persistence keyed per-browser not per-entity — old entity state reused for the new one; must ALSO verify backend actually created a clean row (fetch it raw by id) to rule out server-side leakage. Investigation first, then fix | ✅ done 2026-07-18, web PR #11 — **confirmed 100% frontend, backend ruled out** (created 2 entities via API, raw GET on each returned clean isolated rows). Root cause: `useProfileStore` is a module-level singleton never reset on entity switch, plus `if (biz.x) store.setX(biz.x)` conditional assigns left old values when the new entity's field was empty. Fix: unconditional reset before fetch (name/description/blocks/registry data) + `key={businessId}` on `EditView` to remount child components' own local state (VerificationSection/PublishSection/BlockCard). Live-verified via real SPA navigation between two test entities — no leakage, no false "verified". Deployed, `/profile` 200 | `teta-pi/web` `profile/page.tsx` |
| 3.12 | `3 frontend · 3.12 app chrome (QA #8/#10/#12/#17-app + #11 + #24)` | proper fixed header/menu bar for app (logo currently floats over content); "Under construction" banner → fixed, top z-index, content offset; #12 Share page shows internal system link instead of the public `/e/[slug]` URL; favicon (Θ+π) for app; #11 onboarding "Camera" step shows dead QR/pairing stub — either wire to the real device-link flow (same as profile Connect) or hide the step until 14.x; **#24 (QA session 2, 2026-07-19): `/profile` itself needs the same fixed translucent iOS-style header** (logo + avatar), content offset beneath it, not just the app shell | ✅ done 2026-07-21, web PR #14 — new shared `AppHeader` (Wordmark + `AccountMenu`, translucent/blurred bar) replaces each page's ad-hoc floating logo, used on `/`, `/search`, `/profile`, `/settings`, `/e/[slug]` — satisfies both #10 and #24 (profile gets it directly, not just via a shared shell). Banner is now `position:fixed` with height in CSS var `--banner-h` (mirrors 10.6's landing approach) so pages can offset below it via `calc()`; wraps to 2 lines on narrow mobile instead of clipping mid-sentence (the full sentence doesn't fit even at unreadable font sizes). #17-app: copied 10.6's `favicon.svg`/`apple-touch-icon.png` into Next's `src/app/icon.svg` + `apple-icon.png` (auto-wired, no `<link>` tags needed). #11: Camera-pairing step deleted outright (dead `PseudoQR`/fake pairing code removed, not hidden-in-place) — the wizard now goes Identify → Verify → Publish (3 steps, was 4); entity-creation side-effect (unchanged) still fires as soon as the account exists, its spinner/error moved to the Publish/success screen since Verify now lands there directly. #12: investigated, **already correct since 2026-07-11** (`aef2e4f`) — `SharePageButton` has always built `https://app.tetapi.dev/e/{slug}`; confirmed live slugs are human-readable (`hellfire-solutions`, `shosho`, …) via direct psql, not UUIDs. No reproduction found; likely a stale QA observation or a different surface not identified. **Bonus bug found while verifying #11 in-browser**: the three page-shell wrappers (`/`, `/profile`, claim's `PageShell`) used `overflow:"hidden"` for decorative blur clipping, which — once their content briefly exceeds 100vh (e.g. the claim wizard's sub-kind picker) — makes them a real, invisible scroll container; a focus/layout-shift event scrolled it ~180px internally and permanently hid the fixed chrome above the fold after a step change, with no scrollbar to reveal why. Fixed via `overflow:"clip"` (clips, never scrolls) on those three; also reset `window.scrollTo(0,0)` on claim step change as a defensive UX habit for step wizards. Verified: desktop + mobile (375×812) screenshots of home/search/profile/claim (all 3 steps)/`/e/[slug]`; `npm run build` clean. **Prod-verification caught a real deploy-config bug**: the new icon routes 404'd live despite a clean build — this repo's deploy workflow hand-maintains `app-paths-manifest.json` on the server (5.3-split standalone-output quirk) and didn't list them; fixed in web PR #15, both routes now 200 on `app.tetapi.dev` | `teta-pi/web` `src/components/AppHeader.tsx` (new), `layout.tsx`, `globals.css`, `page.tsx`, `search/page.tsx`, `profile/page.tsx`, `settings/page.tsx`, `login/page.tsx`, `claim/page.tsx`, `e/[slug]/page.tsx`, `AccountMenu.tsx`, `.github/workflows/deploy.yml` |
| 3.13 | `3 frontend · 3.13 profile redesign (QA #15 + session-2 #26/#27/#28/#29/#30/#31)` | ✅ **done 2026-07-24, web PR #16.** Blocks-first layout: name/description → blocks grid (primary object, #29) → compact icon menu, in that order. Verifiers + Registry Match + Document Upload + Legal Entity + Publish&Privacy merged from two full-width-strip components (`VerificationSection`+`PublishSection`) into one `VerifyMenu` — a row of small icon buttons (Registry/Email/Domain/Document/Legal/Publish) with a status dot each, accordion-opens one `MethodCard` panel at a time below the row (#28/#31). Name+description now default read-only with a single Edit→Save toggle (`fieldsEditing` state, `!companyName` seeds it true for new profiles) instead of always-editable fields (#26/#27). `PiCamSection` slimmed to `PiCamButton`, moved into the blocks header row next to "Your blocks" (#30) — same QR/pairing logic, just relocated, ready for 14.5 to reuse as the second sync entry point. Avatar was already in the standard header position from 3.12's `AppHeader`/`AccountMenu` — no change needed there. Added `BuildingIcon`/`GlobeIcon`/`LinkIcon`/`ShieldIcon` to `VerificationIcon.tsx` for the menu row. `/e/[slug]` untouched — it doesn't reference any of the edited components. | ✅ done | `teta-pi/web` `src/app/profile/page.tsx`, `src/components/ui/VerificationIcon.tsx` |
| 3.14 | `3 frontend·1 backend · 3.14 re-investigate entity-state artifacts on FIRST creation (QA #25 + #32)` | 🔴 **Critical — possible incomplete fix or a different code path than 3.11, now confirmed to be a wider pattern.** QA 2026-07-19 #25: right after creating a business page (first entity, not a second one — 3.11 was scoped to switching between two existing entities), "✓ Verified in registry" appears with NO verification performed, plus stray garbage characters/text and a registry number that isn't the user's. **Manager confirmed 2026-07-19 this connects to #32** (originally filed as "delete seed entities" — see 6.3): the "phantom" business names the owner saw in search (Haiku, Vega, Foundry, Ant, etc.) do **NOT exist in the database at all** (0 rows on a direct `psql` query) — meaning the frontend is fabricating entire fake entity cards/names client-side, not just corrupting one real entity's displayed fields. Same *texture* as #18's "Bronnows Sonnet Sonnet Sonnet" garbage. **Do not assume 3.11's fix covers this** — investigate whether the claim flow's live-preview/draft state (shown while filling the multi-step form, or some mock/demo/placeholder dataset used during development) leaks into either the persisted store OR the rendered search results on first real creation, a code path 3.11's "reset on businessId switch" might not touch (there's no *prior* businessId to switch away from on a first creation, and search results are a different component tree entirely from `/profile`). Verify backend is clean first (same method as 3.11: create via API, raw GET, AND a direct psql check like the one that ruled out #32 being real data). Root-cause before fixing | ✅ done 2026-07-20, web PR #13 — **#25**: `useRegistryCheck` fired a live external-registry name search on every company-name keystroke and showed "✓ Verified in registry" with a foreign company's authority/registration number — name-search result ≠ ownership verification. Removed the hook; `registryStatus` now loads from `businessApi.get()` (`business.registry_status`/`registry_data`, DB truth) into `useProfileStore`, EditView/VisitorView/AgentView derive trust state from that. **#32**: home `page.tsx` fell back to 7 hardcoded `SEED_RESULTS` (Vega Energy Markets, Foundry Microworks, Meridian Logistics…) whenever the API returned 0 results or failed, rendering them as if real; fixed to `setApiResults([])` on empty/failed search, seed pool now shown only in the pre-search landing hero, never for a submitted query. 3 files, +26/-61. Prod-verified: `/profile`, `/`, `/search` all 200, no seed-name strings present in home HTML | `teta-pi/web` `profile/page.tsx`, `useProfileStore`, claim flow draft state, search result rendering · read-only backend check |
| 3.15 | `3 frontend · 3.15 "Grid of Record" profile redesign — owner design handoff 2026-07-29` | **Owner delivered a high-fidelity design handoff** (`docs/design/profile-grid-of-record/` — README.md spec + `Profile Grid of Record.dc.html` reference prototype). This **supersedes 3.13's shipped UI** with a different visual language: verification seals become a full-width **attestation bar above everything** (registry/c2pa/btc, re-check timestamp); blocks become a **strict 1:1 square ledger** (not the current free-form blocks-first grid) with drag-reorder, media-type filter chips, hover provenance overlay (seal state + hash + description), and a dashed "ADD BLOCK" tile; verification steps become a **6-up row of low action tiles** (Registry/Email/Domain/Document/Legal/Publish) replacing `VerifyMenu`'s icon-accordion; block click opens a **detail modal** (media + title/desc + fact grid + Verify chain/Replace media actions) in every mode; page has an explicit **3-way mode switch (Edit / Visitor / Agent)** — Visitor mode adds a trust-sentence footer with CTAs, Agent mode renders a literal MCP-response panel (mono, dark bg) — today's app only really has Edit/read-only, no explicit Agent-mode view; mobile layout stacks seals vertically and drops to a 2-col grid. Design tokens (colors/type/spacing) are exact and listed in the README — high fidelity, match closely. **Decomposed into sequential sessions (same file family, must run one at a time, not parallel)**: **3.15a** foundation (nav/attestation bar/identity/facts strip/mode-switch state) → **3.15b** square ledger (statement tile grid, drag-reorder, filter chips, hover overlay, add-block tile) → **3.15c** verification action-tile row (replaces `VerifyMenu`) → **3.15d** block detail modal → **3.15e** Visitor footer + Agent panel (new page states) → **3.15f** mobile responsive layer. Each sub-session should read the full README before touching code — it has exact hex/px values, not to be approximated. **3.15a done 2026-07-29** (`teta-pi/web` PR #20, merged, branch `session/3.15a-grid-foundation`) — built `AttestationBar`/`ProfileIdentity`/`FactsStrip` as new shared components rendered once in `ProfilePage()`, above the Edit/Visitor/Agent switch (regions 1-4 are identical across all three modes per spec, only 5+ differ). Attestation bar's 3 seals + facts strip's 4 stats are all derived from real store/entity data (registry status, per-block `c2pa_verified`/`bitcoin_confirmed`, `verification_level`, `created_at`) — no hardcoding; "agent lookups / 30d" renders "—" since no backend source exists yet (logged below). Removed the now-redundant inline "✓ Verified in registry" status row and the old floating bottom Edit/Visitor/Agent segmented control (superseded by the new inline mode switch in the identity region) — `useProfileStore`'s existing `view: 'edit'|'visitor'|'agent'` field already matched the spec's mode state 1:1, reused as-is. `AppHeader` (region 1) reviewed against spec and left unchanged — it's shared fixed-blur chrome across 5 pages, not page-local nav; forcing its solid-bg/exact-padding literal spec values onto global chrome was judged not worth the cross-page risk this session. Regions 5+ (blocks, `VerifyMenu`) still render in 3.13's old glass style below the new white board, as expected mid-migration.
**3.15b done 2026-07-31** (`teta-pi/web` PR #21, merged, branch `session/3.15b-square-ledger`) — built `StatementTile`/`AddBlockTile`/`LedgerControls`/`StatementLedger` and hoisted them into the shared white board alongside 3.15a's regions, since spec's region 6 (square ledger) shows in **every** mode, not just Edit — `EditView`'s and `VisitorView`'s own separate block-rendering (the last of 3.13's per-mode duplication) is gone; `AgentView`'s JSON panel now appends below the shared ledger instead of replacing it. Real per-block KIND/marks/hash/date all derived from actual data — no fabrication, with two documented adaptations where the real data model doesn't match the mock 1:1: (1) the data model has no "audio" media type (`MediaItem.type` is video\|photo\|file only) and no per-block registry association, so filter chips are `ALL·video·photo·text·file` (not `·audio`) and a tile's seal maxes out at "full chain" with 2 marks (c2pa+btc), not 3; (2) real signed photos render their actual uploaded image in the tile (not just the decorative placeholder stripe) since this app has genuine evidence to show, unlike the design mock — video keeps the spec's literal ink-stripe treatment. Drag-reorder persists via `blockApi.reorder`/`persistBlockOrder`, which **already existed** from 3.13 (`PATCH /blocks/reorder` — no missing endpoint to log, contrary to what this task guessed might be needed). Block content editing (title/description/media upload) — not itself in scope this session — is preserved via `BlockEditPanel` (renamed from `BlockCard`, opened by clicking a tile in Edit mode or after ADD BLOCK), a deliberate bridge so that functionality doesn't regress before 3.15d ships the real click-to-open detail modal; the ledger's own hint text says "click to edit" rather than the spec's "click to inspect" for this reason, and only in Edit mode (non-edit modes show no hint yet, since inspection isn't wired). Verified locally: add/edit/remove a block, all 5 filter chips (real index preserved under filtering), Edit/Visitor/Agent mode switching, attestation bar + facts strip reacting live to new blocks.
**3.15c done 2026-08-01** (`teta-pi/web` PR pending, branch `session/3.15c-verify-tiles`) — replaced `VerifyMenu`'s round-icon-button row (3.13, QA #28/#31) with the spec's 56px-tall 6-up `VerifyActionTile` row (`Registry · Email · Domain · Document · Legal · Publish`), moved from the old 880px glass-styled area into the shared white board itself, positioned between facts strip and ledger controls — the correct spec order (region 5a before 5/6), which the board hadn't followed until now since 3.15b's task didn't touch this region. Reused 3.13's existing SVG icon set (`BuildingIcon`/`MailIcon`/`GlobeIcon`/`DocumentIcon`/`LinkIcon`/`ShieldIcon`) at 18px per the task's own allowance, rather than building new CSS-shape glyphs — same steps, same icons, just recolored per the new state machine. **All verification logic, state, and API calls (`runRegistry`/`sendEmailCode`/`confirmEmailCode`/`getDomainInstr`/`checkDomain`/`linkLegal`/`unlinkLegal`/`doPublish`/`togglePrivacy`, all `MethodCard` panels) are completely unchanged** — only the selector row's chrome was replaced, not rewritten. Tile state machine (verified/in progress/pending) is a literal port of the design's own `verifySteps()` JS, including its non-obvious asymmetry: a verified Publish tile's status text is still purple (`ok` always renders purple status text, regardless of item), while an in-progress tile's status text is always orange (regardless of item) — only the border/glyph color uses the per-item accent (orange for Publish, purple otherwise). Real per-step done state from existing store/API signals (`registryStatus==='verified'`, `emailDone`, `domainDone`, `!!linked`, `isPublished`) — no hardcoded `done` map. Two small, deliberate simplifications vs. the pre-existing 3.13 behavior, both logged here rather than silently dropped: Domain's old distinct green dot accent is retired in favor of the new design's reduced purple/orange-only palette (matches AttestationBar/StatementTile); Publish's "done" now means simply `isPublished` (the old published-but-private nuance, previously shown via a muted vs. green dot, is still visible inside the unchanged panel's status pill, just no longer double-encoded in the compact tile). Grid column count matches the real visible step count (6 for business/organization, 3 for person-kind entities where Registry/Document/Legal don't apply) rather than a hardcoded `repeat(6,1fr)` that would leave gaps. `EditView` (which by 3.15b had shrunk to a bare `<VerifyMenu/>` wrapper) is now fully retired — `VerifyMenu` mounts directly in `ProfilePage()`, keeping its `key={businessId ?? "new"}` remount-on-entity-switch behavior (QA #18). Verified locally: pending/in-progress/verified-eligible states, Publish's orange accent, business (6 tiles) vs. person (3 tiles) gating, mobile 3-column wrap, existing panels (Registry/Email/Domain/Publish) still open and function exactly as before.
**3.15d done 2026-08-02** (`teta-pi/web` PR pending, branch `session/3.15d-block-modal`) — built `BlockDetailModal`, the spec's read-only block inspector (600px panel, media placeholder/real photo, title/desc, 2-col fact grid, `Verify chain`/`Replace media` actions). The tile's `onClick` is no longer Edit-only: every `StatementTile` now opens the modal regardless of mode (`isEdit` still only gates `draggable`/cursor), which is the actual point of this session — clicking a square in Visitor and Agent mode previously did nothing at all. This retires 3.15b's "bridge" (tile click → `BlockEditPanel` directly in Edit mode); that panel is still the only place title/description/media upload happen, just reached one click deeper now, via the modal's "Replace media" action (`onReplaceMedia` closes the modal and sets the same `editingBlockId` state ADD BLOCK already used) — reused, not rebuilt, per the task's explicit instruction. "Replace media" itself only renders in Edit mode (an owner-only action; showing it to a visitor/agent who can't act on it would be misleading) — the spec's own prototype doesn't branch by mode here since it has no real auth, but the real app does. "Verify chain" has no backend for a manual per-block re-check (grepped `verifyApi`/`blockApi` in `lib/api.ts` — only entity-level registry/email/domain endpoints exist, nothing per-block), so it mirrors the design prototype's own stub behavior (click closes the modal, same as the mock) plus an explicit caption underneath saying so, rather than either faking a network call or hiding the button — logged below. Fact grid rows are all real: `type`/`signature`/`attestations` reuse the same `blockKind`/`blockHashLabel`/`blockMarks` helpers the tile's hover overlay already used (extracted `blockHashLabel`/`blockMediaLabel` out of `StatementTile` so both render identically, not two copies of the same string logic); `captured` reuses `formatTileDate`. Media area shows the real uploaded photo when one resolves (same `mediaUrl()`/`showRealImage` pattern as the tile), ink-stripe placeholder for video/text/file. `LedgerControls`' hint text now matches the spec exactly in every mode (`drag a square to reorder · click to inspect` / `click a square to inspect`) instead of 3.15b's placeholder "click to edit" (Edit-only, since inspection wasn't wired yet). Verified locally in a synthetic (unauthenticated, local-only) block across all three modes: modal opens on tile click in Edit/Visitor/Agent, closes on `×`/overlay-click/`Verify chain`, `Replace media` (Edit only) closes the modal and opens `BlockEditPanel` with the same block's title editable, Visitor/Agent modals correctly omit `Replace media`. **3.15e (Visitor footer + Agent panel) next.** | ✅ **3.15d done — 3.15e (Visitor footer + Agent panel) next** | `teta-pi/web` `profile/page.tsx` (`BlockDetailModal` added, `StatementTile`/`StatementLedger` click wiring changed, `blockHashLabel`/`blockMediaLabel` extracted) · design source: `teta-pi/infra` `docs/design/profile-grid-of-record/` |
| 3.16 | `3 frontend · 3.16 "Grid of Record" search redesign — owner design handoff 2026-07-29` | **Owner delivered a second high-fidelity design handoff** (`docs/design/search-home-results/` — README.md spec + `Search.dc.html` reference prototype), same visual language as 3.15 (attestation seals as CSS glyphs, mono labels, strict square evidence tiles, no shadows/rounded cards). Two pages: **Home** (`/`) becomes radically minimal — no hero copy, no feature grid, nav + ~520px of air + one query field on a single `border-bottom` hairline rule (no box) + a mono registry-stats line underneath (`signed evidence only · N entities · N blocks`) — replaces the current hero+search-bar+tagline homepage entirely. **Results** (`/search`) gets a full overhaul: a 4-cell **evidence filter bar** (`all results · full chain · partial chain · declared only`, counts server-computed) above the results, each result row showing attestation seals + a trust badge (`L3 media-backed`/`L2 partial chain`/`L1 declared only`, derived server-side from `marks`, never authored), an answer paragraph describing which blocks matched (template-based, not model-generated per current scope — flag if a model is used instead, it needs its own provenance marker), and an expandable `repeat(4,1fr)` evidence-tile grid reusing **the same tile/modal components as the profile page (3.15b/3.15d)** one size down. Unsigned/unverified mentions are never mixed into the ranked list — withheld behind an explicit "Show unverified" action. Ranking rule is product logic, not decoration: full chain > partial chain > declared-only, never by engagement/payment. Both pages ship a 390px mobile board. **Decomposed into sessions**: **3.16a** Home minimalist redesign (`web/src/app/page.tsx`) — independent of 3.15, can run in parallel with 3.15b now; **3.16b** Results page (`web/src/app/search/page.tsx`) — evidence filter bar, result rows, trust-badge logic, expandable evidence grid — **should boot after 3.15b (and ideally 3.15d) land**, so it reuses the profile's tile/modal components instead of building duplicates; **3.16c** mobile responsive layer for both. Read the full README before touching code — exact hex/px values, not to be approximated.
**3.16a done 2026-07-31** (`teta-pi/web` PR pending, branch `session/3.16a-home-minimal`) — `/` rewritten from scratch: nav (Θ+π + Search/My page/Settings + "Claim your page", collapsing to a `claim page` mono link under 640px) + the query-line-is-the-page region (hairline `border-bottom`, real `<input>` with its native caret — the design's animated caret is prototype-only, dropped per its own README note — SEARCH ↵ triggers `router.push('/search?q=...')` on Enter or click) + the under-rule registry line. Deleted the old hero/tagline/feature-card/footer content entirely (not relocated — the task said remove, not rehome) along with `SEED_RESULTS`/`lib/seedData.ts` and the now-fully-unused `DisplaySearchResult` type, both now-dead code the old home page's inline mini-search used (the same hardcoded fake-entity data flagged in 3.14's #32 finding — this removes it from the codebase entirely, not just from display). "N entities · N blocks" renders "—"/"—" — no public count endpoint exists (checked `lib/api.ts` + `docs/api.md`: only `/claim/stats` and `require_admin`-gated `/admin/stats`), logged in known-issues, same non-fabrication choice as 3.15a's "agent lookups". Verified locally: matches spec at 1280px and 390px, Enter-to-search and click-to-search both route to `/search?q=...` correctly (query encoding, `/search` page itself untouched and still resolves `?q=`), all nav links point at real routes (`/search`, `/profile`, `/settings`, `/claim`). Home page bundle size dropped 5.75kB→1.83kB from removing the dead inline-search code path. | ✅ **3.16a done — 3.16b (results page) can boot now, 3.15b + 3.15d (tile/modal it reuses) both done** | `teta-pi/web` `src/app/page.tsx` (full rewrite), `src/app/search/page.tsx` (untouched), shared tile/modal components from 3.15 · design source: `teta-pi/infra` `docs/design/search-home-results/` |
| 10.5 | `10 landing · 10.5 claims-verification section (+ optional Universal Tag section)` | **owner 2026-07-17 (Landing Update master prompt v3), P1:** new section "Prove the claim. Not just the identity." after How-It-Works/verification-levels, before Pricing. Sub: "We don't verify that a business exists. We verify that what it claims is actually true." 3–4 example cards (sushi chef / tailor / artist / journalist): claim → PI Camera films → C2PA+Bitcoin authenticity → AI checks claim-match. Compact "Two checks, one block" diagram (1 Authenticity: C2PA+BTC timestamp; 2 Claim Match: AI-assisted yes/no/partial). Footer line: "Works for anyone — no legal entity required." **Tone guardrail: AI-assisted signal with manual-review/appeals path, one disclosure sentence — never "AI guarantees truth."** P2 if time allows: brief "Universal Tag" section near Developers/MCP (script-tag + DNS two-step, honest two-part disclosure — see `docs/universal-tag.md`). NOTE: landing is static HTML (`landing/*.html`), NOT Next.js — the master prompt's tech assumption is wrong for tetapi.dev. Don't touch: pricing, TWIRA section, entity-types grid, timeline, EU-AI-Act countdown, 3-audience hero | ✅ done 2026-07-17, landing PR #3 — both P1 claims section AND P2 Universal Tag section shipped, deployed, "Prove the claim" verified live on tetapi.dev. Note for 12.5: when 12.5a–c ship, re-check the Universal Tag section's snippet/copy still matches reality | `teta-pi/landing` `index.html` |
| 10.6 | `10 landing · 10.6 landing polish (QA #8/#9/#17/#21/#22/#23)` | sticky translucent top menu; "Under construction" banner fixed + top z-index (landing side of #8); favicon (Θ+π) for landing + email templates; #21 marketing numbers (MCP server counts etc.) become clickable → authoritative source (live dashboard if one exists); #22 restore article links in the references block (existed before, was dropped); #23 Academic Evidence → clickable arXiv links | ✅ done 2026-07-18, landing PR #4 (11 pages) — sticky/blurred nav (fixed a real `overflow:hidden` breaking `position:sticky` on index.html along the way); banner offset now JS-measured (`--banner-h`), verified desktop+mobile+open-menu; SVG/PNG favicon (Θ+π) live; 4 marketing stats + 3 arXiv citations linked, real URLs restored from git history where they existed. 🟡 owner follow-up: the "13,853 MCP servers" stat has no findable real source (linked to the official registry as best-available) — confirm/refresh it; supply real arXiv IDs for 2 citations if they exist; favicon still missing from email templates (`teta-pi/api`, small separate task) | `teta-pi/landing` `*.html`, favicon assets |
| 10.7 | `10 landing · 10.7 publish whitepaper v1.1` | **owner-authored, 2026-08-01**: `TETAPI_Whitepaper_v1.1.pdf` (Bob V. & Mykhailo M., "Trust Infrastructure for Digital Entities" — registry model, claims-based verification, TWIRA ranking algorithm, Temporal Moat, roadmap, governance). Publish as a static asset + a dedicated `whitepaper.html` landing page (or equivalent) linking to it; add a discoverable link from `index.html` (nav or footer) and `for-agents.html`/`developers.html` (it's citable technical documentation, agents should be able to find it too — consider an `llms.txt` mention). Local source: `~/Downloads/TETAPI_Whitepaper_v1.1.pdf` | ✅ done 2026-08-03, landing PR #8 — `whitepaper.html` (About-page template: hero, abstract teaser pulled verbatim from the PDF, "What's inside" 6-tile summary, PDF read/download CTAs), PDF published flat at `/TETAPI_Whitepaper_v1.1.pdf` (matches existing top-level static-asset convention, no `assets/` dir in this repo). Discoverable from `index.html` footer (Resources column), a callout section on `for-agents.html`, a tile in `developers.html`'s open-source grid, `llms.txt`, and `sitemap.xml`. Nav kept to the site-wide standard 5-item set (About/For Businesses/For Agents/Registries/Start) rather than inventing new global nav items — `developers.html` itself is also not in that nav, confirmed as the existing pattern. PDF content untouched. Verified live: `whitepaper.html` 200, PDF 200 with `Content-Type: application/pdf` (129300 bytes), all four link surfaces resolve on prod | `teta-pi/landing` `whitepaper.html` (new), `TETAPI_Whitepaper_v1.1.pdf` (new), `index.html`, `for-agents.html`, `developers.html`, `llms.txt`, `sitemap.xml` |
| 11.1 | `11 backoffice · 11.1 build dashboard v2` | owner dashboard per the 8.1 design (took over 8.2): Dashboard tab in `/admin` + thin `GET /admin/health-check` | ✅ done 2026-07-12, PR #16 | `web/src/app/admin/page.tsx`, `web/src/lib/api.ts`, `routes/admin.py` (append) |
| 11.2 | `11 backoffice · …` | next backoffice tasks defined by owner/manager (e.g. claims ops tooling) | ⚪ open | `/admin` UI + admin routes |
| 12.1 | `12 wordpress · 12.1 plugin MVP` | **owner priority — first public release.** TETA+PI WordPress plugin, free tier: connect the WP site to a TETA+PI entity (`pk_live_` key), auto domain-ownership verification (plugin serves `/.well-known/tetapi-verify.txt`, calls `/verify/domain/start`+`/check`), verified-badge widget/shortcode. Define the $25 premium pack (aligned with Module #1 pricing) — PLAN the pack contents first, then build free tier fully + premium as licensed stubs. No server-side changes | ✅ done 2026-07-13, PR #28 | new `wordpress-plugin/` only (excluded from deploy.yml) |
| 12.2 | `12 wordpress · 12.2 publish to wordpress.org` | plugin review submission, listing copy (assets/screenshots/readme.txt), $25 pack purchase flow decision (license key via claim/Stripe — owner to pick); landing gets a "WordPress" mention (goes through 10.x) | ⏳ after 12.1 · owner approves the actual publish | `wordpress-plugin/` + listing metadata |
| 12.5 | `12 wordpress · 12.5 Universal Tag (non-CMS sites)` | **owner 2026-07-17: spec PDF delivered → transcribed to `docs/universal-tag.md`.** One script tag (GA-style install) + optional DNS record for the ~29–48% of the web with no CMS. Two-part model (honest disclosure mandatory): A = `tag.js` JSON-LD injection + single tag-ping beacon (JS-executing agents only); B = CNAME/TXT + one-line host redirect → real `/.well-known/*` + `llms.txt` reverse-proxied via `verify.tetapi.dev`, generated dynamically. Phased build in that doc: **12.5a** tag.js+/generate (landing) ∥ **12.5b** `/v1/tag-ping` + wk-generator (api, 🟡 droplet-load: public unauthenticated sustained-write endpoint — rate-limit + cheap counter, storage shape (a) file-log vs (b) DB = owner call, same as 2.4) → **12.5c** `verify.tetapi.dev` subdomain + nginx + scheduled DNS checker (**prod-affecting**) → **12.5d** per-host docs + landing section (with 10.5 P2). No dependency on 6.2/GTM Phase 0 | **12.5a ✅ done 2026-07-18, landing PR #5** — `tag.js` (~2.7KB, vanilla JS, no deps: JSON-LD injection + `sendBeacon` to `/v1/tag-ping` with silent catch) + `generate.html` (snippet generator + honest Part A/B disclosure). **12.5b ✅ done 2026-07-19, api PR #12** — `POST /v1/tag-ping` (always 204, in-memory rate limit, bounded Redis sorted set per entity — storage shape decided, `docs/decisions.md`) + `GET /wk/{entity_id}/agent.json`\|`agent-card.json`\|`llms.txt` (5-min cache). ⚠️ Still not live end-to-end: `/wk/*` isn't reachable from a real domain until 12.5c's `verify.tetapi.dev` nginx vhost exists, and JSON-LD field-mapping against `/businesses/{id}/preview` needs a real entity + Google Rich Results Test once wired. 12.5c/d still ⏳ | per phase: `teta-pi/landing`, `teta-pi/api`, server nginx |
| 13.1 | `13 gtm · 13.1 GTM plan → docs/gtm.md` | transcribe + operationalize the owner's **Autonomous GTM Plan** (July 2026 PDF, confidential) into `docs/gtm.md`: Phase 0 self-registration (6 MCP registry listings — overlaps 2.5, which does the tech side), self-verification dogfooding (TetaPi GmbH L2 + founders as person entities, public proof page), Phase 1 agent discovery (instrument tool calls — needs a lightweight-logging decision vs deferred 2.4; proof_url in every response; Show HN/Discord owner-gated), Phase 2 top-500 pre-verification + claim outreach (guardrails: public data only, instant opt-out, one message, no spam; hard dep: claim flow live), Phase 3 loops (badge SVG endpoint `tetapi.dev/badge/{id}` = new small backend task, C2PA/PI-Camera loop → dir 14, cross-verification notifications). Map each item → session/owner, flag droplet-load items | ✅ done 2026-07-13, PR #25 (spawned: 1.10 badge endpoint, 1.11 bulk import, 2.6 proof_url, 8.4 gtm metrics, 10.4 llms.txt, 13.3 dataset script) | `docs/gtm.md` |
| 13.2 | `13 gtm · 13.2 GTM machine v1` | implement approved 13.1: Phase 0 execution support (listings metadata with 2.5), instrumentation + top-500 dataset script (off-server), badge endpoint task handoff to backend, outreach queue with owner-approval gate | ⏳ after 13.1 + 2.5 | per 13.1, off-server scripts/ |
| 14.1 | `14 camera · 14.1 Pi CAM → full integrated product` | **CORRECTED 2026-07-12 after checking the PI CAM session/app state:** Pi CAM already EXISTS — React Native + Expo app (iOS/Android), offline C2PA signing via Secure Enclave/Keystore, own project at `~/Downloads/PI CAM` (own chat session `PI CAM`), already wired to us: `modules/account` (QR link, `pk_live_`, entity binding) uploads via existing `POST /media/device-upload`. Task = FINISH + VERIFY the integration end-to-end: device-upload → server-side C2PA/OTS verification → verified block on profile + `/e/[slug]` → proof via `/proof` + MCP; fix the QA finding (Pi CAM "Connect" on `/profile` silently no-ops when token is null); GTM C2PA-loop: manifest links the creator's public TETA+PI profile. App-side work runs in the PI CAM session (own codebase, NOT this monorepo); platform-side bits (profile Connect flow, device-upload hardening) run here. ⚠️ Pi CAM dir is NOT a git repo — first step: `git init` + private GitHub repo | ✅ platform side merged (PR #27, device-upload → block chain); repo transferred to `teta-pi/pi-cam` (see [[repo-architecture]] memory, 2026-07-14) | app: `teta-pi/pi-cam` · platform: `routes/media.py`, profile Connect UI |
| 14.2 | `14 camera · 14.2 pre-GTM camera sync QA` | **owner 2026-07-14: verify live before GTM launch, runs in PARALLEL to 6.2 (different codebase/owner track).** Full device flow on a real phone against prod: QR-link → device registration → capture photo/video → C2PA sign → `device-upload` → verified block appears on `/profile` and `/e/[slug]` with `c2pa_verified=true` → proof visible via `GET /proof` + MCP `teta_get_proof`. Also verify the known QA finding: `/profile` "Connect Pi CAM" no-ops when `token === null` (login/settings users, not just claim-flow users) — confirm fixed or still open. Log defects to `docs/known-issues.md`, don't silently fix | 🟡 parallel to 6.2, not blocking it | app: `teta-pi/pi-cam` · platform (if bugs found): `routes/media.py`, `web/…/profile/page.tsx` |
| 14.3 | `14 camera · 14.3 release: TestFlight (iOS) + free Android build` | **owner 2026-07-17: bring Pi CAM to real users** — (1) production EAS build config in `teta-pi/pi-cam` (app.json/eas.json: bundle ids, icons, splash, permissions strings, prod API URLs); (2) iOS → **TestFlight** via `eas build` + `eas submit`; (3) Android → free distribution: `eas build -p android` APK, download link on landing (Play Store optional later). **Owner prerequisites, session cannot do these:** Apple Developer Program membership ($99/yr — REQUIRED for TestFlight, no workaround) + Apple ID creds in EAS; optional Google Play Console ($25 one-time) only if Play listing wanted — plain APK needs nothing. ⚠️ hard dep: 14.2's device QA (steps 1–5) has NEVER passed on real hardware — do a full capture→upload→proof pass on a physical phone as part of the release build validation, don't ship untested | ⏳ owner sets up Apple account first · session preps everything else | `teta-pi/pi-cam` (app.json, eas.json, assets) · landing APK link via 10.x |
| 14.4 | `14 camera · 14.4 Pi CAM app won't launch (QA #19)` | 🔴 **Critical, blocks all camera QA (14.2) and the 14.3 release:** the mobile app doesn't start/load at all. Diagnose the build (Expo/EAS, dependency drift since last run, native module issue), get it launching on at least one platform, document exact run steps in the repo README | 🔄 diagnosed + deps fixed 2026-07-18, pi-cam PR #2: install/bundle are clean (expo-doctor 18/18, tsc clean, full iOS prod bundle OK); real blocker = **Expo Go can't run reanimated 4 / New Architecture** (upstream reanimated#8235) — a dev-client build is required. **Owner step to close: `eas build --profile development --platform ios` (Expo account only, no local Xcode) → confirm boot on device → re-run 14.2.** README now documents both paths | `teta-pi/pi-cam` |
| 15.1 | `15 security · 15.1 threat model + red-team harness` | STANDING direction, first task: (1) write `docs/security.md` — threat model (assets: entity data, `pk_live_` keys, admin routes, append-only tables, C2PA/OTS proofs, media store; trust boundaries: agent↔MCP, browser↔API, device↔`/media/device-upload`, WP plugin↔API), attacker classes, per-surface checklist (authn/authz, IDOR, SSRF, path traversal, injection, rate-limit, secrets exposure); (2) triage the 6.1 audit's security findings (🔴 #1 path traversal, 🟠 #7 SSRF, #11 fake client verify) as the seed backlog, mapped to the backend fix tasks (1.6-1.9); (3) design the RECURRING loop — a read-only authorized audit re-run cadence + CI security scanning (CodeQL / `npm audit` / `bandit` — runs on the GitHub runner, ZERO server load) to add in 15.2. **Authorized, our-own-infra only; no destructive/DoS/exfil tests against prod; findings reported, not exploited.** | ✅ done (2026-07-18) — `docs/security.md` written (assets, trust boundaries incl. new tag.js↔`/v1/tag-ping` B5, attacker classes, per-surface checklist); 6.1 #1 traversal + #7 SSRF triaged CLOSED (api PR #3), #11 fake verify + QA #1/#18 seeded as backlog mapped to 3.9/3.11; 15.2 CI loop (CodeQL JS+Py / npm audit / bandit / secret-scan, runner-side) + re-audit cadence designed | `docs/security.md` (new), `docs/known-issues.md` (append) |
| 15.2 | `15 security · 15.2 CI security scanning` | implement the 15.1 loop: add CodeQL (JS+Python) + dependency-audit workflows to `.github/workflows/` (runner-side, no server load); wire results into `docs/security.md` tracking. Recurring after that | ✅ done 2026-07-18 — api PR #9 (CodeQL+pip-audit+bandit), web PR #9 (CodeQL+npm-audit), mcp PR #4 (CodeQL+npm-audit), landing PR #6 (gitleaks secret-scan); infra `docs/security.md` §6 updated. All non-blocking (push+weekly, no required checks) — bandit/pip-audit already surfaced findings on the first api run, worth a look | `.github/workflows/*` (4 repos), `docs/security.md` |

## Repo routing — which repo each direction lives in (READ BEFORE BOOTING A SESSION)
Post-5.3 split, code lives in **6 active repos**. The old mono `teta-pi/platform`
is **ARCHIVED (read-only, 2026-07-17)** — GitHub rejects pushes to it, so a
session that clones it by mistake simply cannot land work. `protocol`,
`mcp-server`, `pi-camera` are also archived placeholders — never use them.

| Direction | Repo | Clone URL |
|---|---|---|
| 1 backend · 4 db | `teta-pi/api` | `https://github.com/teta-pi/api.git` |
| 2 mcp | `teta-pi/mcp` | `https://github.com/teta-pi/mcp.git` |
| 3 frontend · 8 analytics · 11 backoffice | `teta-pi/web` | `https://github.com/teta-pi/web.git` |
| 10 landing | `teta-pi/landing` | `https://github.com/teta-pi/landing.git` |
| 12 wordpress | `teta-pi/wordpress-plugin` | `https://github.com/teta-pi/wordpress-plugin.git` |
| 14 camera | `teta-pi/pi-cam` | `https://github.com/teta-pi/pi-cam.git` |
| 5 devops · 6 manager · 7 github · 13 gtm · 15 security · all docs | `teta-pi/infra` | `https://github.com/teta-pi/infra.git` |

**Every boot MUST open with the exact `git clone <url> <path>` for the right repo
and a `git remote -v` sanity check.** 12.5 spans repos — each phase (a landing /
b api / c server) is its own session in its own repo.

## Coordination rules (so parallel sessions don't break each other)
- Each session touches **only its own files** (table above). Never edit another
  session's file.
- Shared-risk files: `web/src/lib/api.ts` (sessions 3 & 6) — **only append new
  functions at the end**, don't touch existing lines → merges cleanly.
- `routes/*` — each session takes its **own** file (auth / blocks / admin), never a
  peer's.
- Claude **commits + pushes automatically**; deploy is automatic on push to `main`.
  After each merge the **manager verifies on prod**.
- Each session ends with the `Done / Changed / Risk / Next` block and updates the
  matching `docs/*.md`.

## Server capacity — RESOLVED 2026-07-13
Prod droplet resized `s-1vcpu-512mb-10gb` → **`s-1vcpu-2gb`** (2GB RAM / 50GB
disk, $12/mo) per the 9.1 runbook. Pre-flight snapshot taken
(`pre-resize-2026-07-13`, restorable if ever needed). Post-resize verified:
RAM 1.9GB (798MB free, swap 0/2GB, was swapping 323MB at 458MB total before),
disk 6.8/48GB = 15% (was 78%), all services `active`, all 4 subdomains 200.
The sustained-load restriction below is LIFTED — 5.1, 5.3, Redis #12, SSE
streaming (2.3), 2.4 usage analytics are all clear to proceed on capacity
grounds. (OpenAI billing for 5.1 is now paid too — 5.1 shipped 2026-07-27.)

## Blocked — waiting on keys / DNS (don't start until provided)
| Item | Needs | Effect when unblocked |
|---|---|---|
| ~~Turn on TWIRA semantics (5.1)~~ | ✅ **DONE 2026-07-27** — OpenAI billing paid, key live; backfill run + embed-on-write and semantic `/resolve-intent` verified on prod (REST + MCP) | semantic search + `/resolve-intent` + block embeddings are live |
| ~~Resend domain verification (#11)~~ | ✅ **RESOLVED 2026-07-17** — `tetapi.dev` verified in Resend (DKIM/SPF via Cloudflare auto-config); test send from `verify@tetapi.dev` to a non-owner address accepted. Remaining code step = 1.19 (`FROM_ADDRESS` still `onboarding@resend.dev`) | emails reach everyone once 1.19 lands |
| Ukraine registry | `OPENDATABOT_API_KEY` | UA registry search works (verifier already written) |

---

## Backlog — ordered by what unblocks the most

### Now — fix what's broken (from known-issues)
1. ✅ **Persist profile blocks to the backend** — load entity+blocks on open, save
   add/edit/remove via the API. *(Done 2026-07-06, `6a022bb`. Reorder UI = session 3.)*
2. ✅ **Remove/gate `/auth/register`** — dead, unauthenticated. *(Done 2026-07-06.)*
3. ✅ **Turn on TWIRA semantics** — embedding CODE merged 2026-07-11 (PR #3: embed on
   block create/update + `twira_backfill_block_embeddings` task). *(Done 2026-07-27, 5.1:
   billing paid, backfill run via the live Celery worker, embed-on-write + semantic
   `/resolve-intent` verified on prod, REST + MCP.)*
   - 🟡 also open: `GET /businesses/{id}/blocks` leaks private blocks (session 2);
     in-memory rate-limit/lock assumes single worker → move to Redis before scaling (#12).

### Next — the MCP investment (owner's priority)
The differentiator: make TETA+PI the registry agents actually route through
(see `docs/mcp.md`).
4. ✅ **Enrich `teta_resolve_intent`** — returns `first_verified_at`, proof URLs, and the
   full T/I/P breakdown in a shape agents can rank on; adds `entity_types` +
   `min_trust` filters. *(Done, MCP 1.2.0, merged.)*
5. ⏳ **`teta_get_proof` depth** — include OTS status, btc_timestamp_depth, C2PA chain
   length so agents can set their own trust threshold. *(Not started — was bundled with
   the MCP session but only resolve_intent shipped.)*
6. **Agent-facing auth for MCP writes** — design how a verified agent authenticates
   to the MCP server (scoped `pk_live_` keys) before adding any write tools.
7. **Streaming / batched search** for large result sets over SSE.
8. **MCP usage analytics** — which tools agents call, latency, so we tune TWIRA
   weights from real `(query, clicked_entity)` pairs (the data moat closing).
   *(Bundle with the MCP session — it touches the MCP server; do not run parallel to #4/#5.)*

### Product — account & sharing
9. ✅ **"Share page" button** on `/profile` linking to `/e/[slug]` (+ copy link).
   Shown only when the entity is published. *(Done 2026-07-06.)*
10. **Sessions list with devices** — needs server-side session storage (JWT is
    stateless today); "log out everywhere" already works via token_version.
11. Resend domain verification so emails reach everyone. *(Blocked on DNS.)*
11b. **Camera capture → C2PA + OTS notarization** (session 5) — capture photo/video
     through the camera and run it through the EXISTING C2PA signing + OpenTimestamps
     pipeline (blocks already carry `c2pa_manifest` / `ots_proof`). Three use-cases:
     proof-of-creation (content made by a verified entity), proof-of-process (video
     verification of production / standards compliance), and copyright deposit
     (timestamp as priority evidence). Reuse the proof services — do not build a
     parallel pipeline. Scaffold capture UI as new files under `web/src/app/capture/`;
     plan first, then wire. *(Frontend + Backend.)*

### Back office — internal analytics
15. 🔄 **System metrics dashboard** (session 6) — on top of the existing `/admin/stats`
    (snapshot counters) and `/admin/analytics` (GoatCounter traffic), add: time-series
    trends (entity growth + verification_events per day), entities by `entity_type`,
    a claim → verified funnel, and registry search health (success rate / latency).
    Read-only aggregation, `require_admin` + audit, no new tables. MCP-usage analytics
    stays out of scope here (that is #8).

### Platform — scale readiness
12. 🟠 Move rate limiters + Handelsregister lock to Redis (unblocks multi-worker).
13. More US state registries (CA, TX, DE-state portals) via the `us_states.py` pattern.
14. Learned TWIRA weights (logistic regression on click logs) once MCP analytics exist (#8).

---

## How to pick up an item
New session → read `CLAUDE.md` + the docs named in the item → do just that item →
update docs + append to `known-issues.md` → `changelog.md` → commit + push → `/clear`.
